export enum Http {
  Ok = 200,
  Created = 201,
  Bad = 404,
}
export enum School {
  Admin = "admin",
  Teacher = "teacher",
  Student = "student",
}
export enum Food {
  Admin = "admin",
  Client = "client",
  Dispatcher = "dispatcher",
  Vendor = "vendor",
}

export enum voiceStatus {
  Alerted = "Successfully Alerted Your Emails!!!!",
  NotAlerted = "No Security Action Taken Yet",
}
